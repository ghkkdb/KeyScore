# KeyScore 曲谱制作规则

本文只说明 KeyScore 文本曲谱的书写规则。曲谱使用 UTF-8 编码的 `.txt` 文件。

## 1. 基本结构

一份曲谱由文件头和曲谱正文组成：

```text
@title 示例曲谱
@bpm 100
@beat 4/4
@section_gap 2

1 2 3 4 | 5 6 7 H1 |
---
H1 H2 H3:2 | 0:2 |
```

文件头字段：

| 字段 | 含义 | 规则 |
|---|---|---|
| `@title` | 曲名 | 建议填写；允许包含空格 |
| `@bpm` | 每分钟拍数 | 必须是 `20～400` 的整数；省略时为 `100` |
| `@beat` | 拍号 | 使用 `分子/分母`，例如 `4/4`、`3/4`、`6/8`；省略时为 `4/4` |
| `@section_gap` | 段落停顿拍数 | 必须大于 `0`；省略时为 `1` 拍 |

文件头通常集中写在正文之前，每个字段独占一行。

## 2. 分隔与排版

所有正文单位必须使用空格分隔：

```text
1 2 3 4 5      正确
12345          错误
```

普通换行和空行只负责排版，不产生停顿。下面两种写法的时间结果相同：

```text
1 2 3 4
5 6 7 H1
```

```text
1 2 3 4 5 6 7 H1
```

小节线 `|` 也只负责排版，不占用拍数：

```text
1 2 3 4 | 5 6 7 H1 |
```

当前格式没有注释语法。正文中不要加入歌词、括号说明或其他自然语言。

## 3. 基础音符

数字 `1～7` 表示中音区七个音级：

| 写法 | 音名 |
|---|---|
| `1` | Do |
| `2` | Re |
| `3` | Mi |
| `4` | Fa |
| `5` | Sol |
| `6` | La |
| `7` | Si |

`0` 是休止符，不是音高。

## 4. 五个音区

音区前缀写在音级数字之前：

| 前缀 | 音区 | 示例 |
|---|---|---|
| `LL` | 倍低音 | `LL1`、`LL5` |
| `L` | 低音 | `L1`、`L5` |
| 无前缀 | 中音 | `1`、`5` |
| `H` | 高音 | `H1`、`H5` |
| `HH` | 倍高音 | `HH1`、`HH5` |

前缀必须使用大写英文字母。高音 Do 写作 `H1`，不使用第八音级 `8`。

```text
LL5 LL6 LL7 | L1 L2 L3 | 1 2 3 | H1 H2 H3 | HH1 HH2 HH3
```

## 5. 半音

升号 `#` 写在音区前缀之前：

```text
#1 #2 #4 #5 #6
#L1 #L2
#H4 #H5
#LL1 #HH6
```

规则：

- `#3` 与同音区的 `4` 等音，会规范为 `4`。
- `#7` 与高一个音区的 `1` 等音，例如 `#7` 等同于 `H1`。
- `#HH7` 超出五音区范围，不能使用。
- 休止符不能带升号，`#0` 是错误写法。

## 6. 时值

没有时值后缀的音符、和弦或休止符默认占一拍。使用 `:总拍数` 指定其他时值：

```text
1          一拍
1:2        两拍
1:1.5      一拍半
1:0.5      半拍
1:0.25     四分之一拍
0:2        休止两拍
[1 3 5]:2  和弦占两拍
```

时值规则：

- 时值必须大于 `0`。
- 音符正文的时值只能写非负整数或小数，例如 `2`、`0.5`、`1.25`。
- 不要在音符后写分数形式，例如 `1:1/2` 无效，应写作 `1:0.5`。
- 冒号后的数值是总拍数，不是在默认一拍之外追加的拍数。

相邻内容按书写顺序依次占用时间。例如：

```text
1:2 0:0.5 2
```

表示 Do 占两拍，随后休止半拍，再进入 Re。

## 7. 增时线

独立的 `-` 将前一个音符、和弦或休止符延长一拍：

```text
1 - - -          等价于 1:4
[1 3 5] -       等价于 [1 3 5]:2
0 -              等价于 0:2
1 - | - - 2     增时线可以跨越小节线
```

增时线必须与其他内容使用空格分开。曲谱开头或段落停顿之后不能直接写 `-`。

连续同音如果只需要触发一次，应写成一个长音：

```text
1:2      触发一次并占两拍
1 -      与 1:2 等价
1 1      两个独立音符，各触发一次
```

## 8. 休止与段落停顿

精确休止使用 `0`：

```text
0        休止一拍
0:0.5    休止半拍
0:2      休止两拍
```

段落停顿使用独占一行的 `---`，时值由 `@section_gap` 决定：

```text
@section_gap 2

1 2 3 4 |
---
5 6 7 H1 |
```

上述 `---` 产生两拍停顿。`---` 不能与音符写在同一行，也不能出现在连音组内部。

## 9. 和弦

方括号中的音符从同一拍位开始：

```text
[1 3 5]
[1 3 5]:2
[H1 H3 H5]:0.5
[#1 #2 #5]
```

和弦规则：

- 和弦至少包含一个有效音符。
- 和弦内部不能包含休止符 `0`。
- 和弦内所有音符共享同一个时值。
- 时值只能写在右方括号之后。
- 和弦成员之间必须使用空格分隔。

```text
[1 3 5]:2       正确
[1:2 3 5]       错误：成员不能单独指定时值
[1 0 5]         错误：和弦内不能包含休止符
```

## 10. 不同音符连音

圆括号表示相邻的不同音符或和弦需要无缝衔接：

```text
(1 2 3) 4
(H1:0.5 H2:0.5 H3)
(1 [2 4] | 3)
```

连音规则：

- 每组至少包含两个音符或和弦。
- 圆括号不改变任何成员的时值和起点。
- 可以跨普通换行和小节线 `|`。
- 不能嵌套。
- 不能包含休止符 `0` 或段落停顿 `---`。
- 同音延长应使用 `:总拍数` 或 `-`，不使用圆括号。

```text
(1 2)       正确
(1 [2 4])   正确
(1 0 2)     错误
((1 2) 3)   错误
(1)         错误
```

## 11. 完整示例

```text
@title 示例曲谱
@bpm 100
@beat 4/4
@section_gap 2

1 2 3 4 | 5 6 7 H1 |
H1 H2 H3:2 | 0:2 |

(L5 L6 L7 1) | #1 #2 3:2 |
---
[1 3 5]:2 | [H1 H3 H5] - |
```

## 12. 常见错误

| 错误写法 | 原因 | 正确示例 |
|---|---|---|
| `12345` | 音符没有用空格分隔 | `1 2 3 4 5` |
| `l1`、`高1` | 音区前缀格式错误 | `L1`、`H1` |
| `8` | 不存在第八音级 | `H1` |
| `1:1/2` | 音符时值不接受分数形式 | `1:0.5` |
| `[1:2 3]` | 和弦成员不能单独指定时值 | `[1 3]:2` |
| `(1 0 2)` | 连音组不能包含休止符 | `(1 2)` |
| `1 --- 2` | 段落停顿没有独占一行 | 将 `---` 单独写一行 |
| `@bpm 10` | BPM 超出允许范围 | `@bpm 100` |

## 13. 完稿检查

1. 文件使用 UTF-8 编码并保存为 `.txt`。
2. 所有音符、和弦、小节线和增时线之间都有空格。
3. BPM 是 `20～400` 的整数。
4. 音区前缀使用大写 `LL`、`L`、`H`、`HH`。
5. 音符时值大于 `0`，并使用整数或小数。
6. 和弦成员没有单独时值，也不包含休止符。
7. 连音组至少包含两个成员，没有嵌套或休止符。
8. 需要停顿的地方明确使用 `0` 或独占一行的 `---`。
