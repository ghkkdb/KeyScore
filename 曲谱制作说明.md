# KeyScore 曲谱制作说明

## 1. 曲谱文件

KeyScore 曲谱是 UTF-8 编码的 `.txt` 文本文件，可以直接在软件的独立曲谱编辑窗口中制作。

推荐的文件结构如下：

```text
@title 曲谱名称
@bpm 100
@beat 4/4
@section_gap 2

1 2 3 4 | 5 6 7 H1 |
---
1 2 3 4 |
```

- `@title`：曲谱名称，会显示在歌单中。
- `@bpm`：播放速度，允许范围为 20～400。
- `@beat`：拍号，目前用于显示，不会改变时值计算。
- `@section_gap`：独立一行的 `---` 所产生的段落停顿拍数，默认值为 1。
- 空行和换行只用于整理版面，不会产生停顿。
- 音符之间必须使用空格分隔。
- 小节线 `|` 只用于排版，不影响播放。

## 2. 在软件中制作

1. 启动 KeyScore。
2. 点击“新建曲谱”，程序会打开尚未写入曲谱库的内存草稿，默认包含一个中音 `1`。
3. 在独立编辑窗口中填写曲名、速度和音符；如果直接关闭并放弃修改，不会生成曲谱文件。
4. 查看窗口下方的实时语法检查结果。
5. 点击“保存”，确认后写入曲谱并关闭编辑窗口，曲谱会自动出现在左侧歌单中。
6. 在主界面选择曲谱；搜索框右侧的排序图标可在“最近修改时间”和“名称”之间切换，名称排序同时支持英文 A–Z 和中文拼音首字母，然后切换到游戏乐器界面。
7. 按播放快捷键（默认 `F9`），等待设定的倒计时后开始播放。
8. 播放中再次按播放快捷键暂停或继续，按停止快捷键（默认 `F10`）停止。

编辑器上方提供总时值快捷按钮，可以把光标所在或选中的音符、休止符、和弦直接设置为 ¼、½、¾、1、1½、2 拍，也可以输入自定义总拍数。卷帘中的“新音符拍数”默认提供 1/4、1/2、3/4、1、2、4 拍；默认使用 `D` 正向切换、`A` 反向切换，用户可以在“设置 → 快捷键”修改常用列表、循环顺序、默认值和两个切换快捷键。卷帘转换为曲谱文本时会按拍号插入小节线，并每四小节换行。设置总时值会替换原有的 `:时值`，并清除属于目标音符的后续增时线。编辑器还提供“小节 |”“延长 1 拍”“休止 ½ 拍”“休止 1 拍”“休止 2 拍”和“段落停顿”等快捷插入按钮。

保存时，本地 `.txt` 文件会根据 `@title` 自动命名。Windows 文件名不允许的字符会被移除；如果已有同名文件，则自动追加 `2`、`3` 等数字，绝不会覆盖原曲谱。

也可以用其他文本编辑器制作 `.txt` 文件，再通过“导入”复制到 KeyScore 曲谱库。

### 2.1 从游戏演奏录制曲谱

“曲谱”页面和顶部工具栏均提供录制入口。录制前需要指定曲名、BPM、拍号、量化精度和和弦容差；切换到游戏后会按“设置 → 外观”中的时长进行倒计时。按录制快捷键（默认 `F8`）完成，按停止快捷键（默认 `F10`）紧急停止并整理已有内容。

录制器按照当前按键配置反向解释输入，因此开始前必须先选择正确的 Profile。同一个物理按键不能同时映射到两个音符；当前在“设置 → 快捷键”配置的三个全局控制键主键也不能作为该 Profile 的演奏键。多个音符在和弦容差内按下会生成 `[1 3 5]`，达到拍号边界时自动插入 `|`。

- 短按触发：用相邻音符的按下间隔确定曲谱时值，不根据短暂的松键时间猜测休止。
- 持续按住：用按下到释放确定发音时值，空白部分生成休止；相邻单音无缝衔接时可自动生成 `(1 2)`。
- 游戏失去前台：立即暂停录制并关闭仍按住的音符；切回原窗口后继续，暂停时间不会写入曲谱。

停止后会打开可编辑的“录制整理”窗口。保存前可以直接修正量化误差，保存后还会进入完整曲谱编辑器。

## 3. 基础音符

```text
1 2 3 4 5 6 7
```

| 写法 | 含义 |
|---|---|
| `1` | 中音 Do |
| `2` | 中音 Re |
| `3` | 中音 Mi |
| `4` | 中音 Fa |
| `5` | 中音 Sol |
| `6` | 中音 La |
| `7` | 中音 Si |

普通音符不按音区修饰键。数字对应的实际游戏按键可以在“设置”中修改。

上标简谱中的高音 Do，例如 `¹1` 或 `1̇`，制作 KeyScore 曲谱时写成 `H1`。

## 4. 五个音区和半音

在数字前添加前缀：

```text
LL1 LL2 LL3   倍低音 Do、Re、Mi
L1 L2 L3    低音 Do、Re、Mi
H1 H2 H3    高音 Do、Re、Mi
HH1 HH2 HH3   倍高音 Do、Re、Mi
#1 #2 #4 #5 #6    中音的五个独立半音
#LL1          倍低音半音
#L1          低音半音
#H1          高音半音
#HH1          倍高音半音
```

- `LL`：倍低音区，只能用于五行或每音独立映射。
- `L`：演奏时按住设置中的低音修饰键。
- `H`：演奏时按住设置中的高音修饰键。
- `HH`：倍高音区，只能用于五行或每音独立映射。
- `#`：演奏时按住设置中的半音修饰键。
- `#3` 与同音区 `4` 等音，`#7` 与高一音区 `1` 等音，解析时会自动规范化；它们不提供独立映射槽位。
- 前缀必须使用大写英文字母 `L`、`H`。

高音 Do 统一写作 `H1`，即“高音修饰键＋Do 键”；曲谱中不使用独立的第八音级。

三行音区直接映射只使用 `L1～L7`、`1～7`、`H1～H7`。五行音区直接映射增加 `LL1～LL7` 和 `HH1～HH7`，这两种音区行模式都不支持半音。需要五个音区的半音时，应使用“每个音符直接映射”。

## 5. 时值和休止

音符默认占一拍。在音符后添加 `:时值` 可以改变其占用拍数：

```text
1          一拍
1:2        两拍
1:0.5      半拍
1:0.25     四分之一拍
0          休止一拍
0:2        休止两拍
```

时值必须大于 0，只能使用整数或小数，不能写成 `1/2`。

冒号后的数字表示音符或休止符的**总拍数**，不是在默认一拍之外额外增加的拍数：

```text
1:2        Do 总共持续两拍，不是持续三拍
1:1.5      Do 总共持续 1.5 拍，相当于默认一拍再延长 0.5 拍
0:2        总共休止两拍
```

也可以使用传统简谱增时线。每个独立的 `-` 将前一个音符、和弦或休止符延长一拍：

```text
1 - - -          等价于 1:4
[1 3 5] -       等价于 [1 3 5]:2
0 -              等价于 0:2
1 - | - - 2     增时线可以跨越小节线
```

增时线必须用空格与音符分开。曲谱开头、段落停顿 `---` 之后不能直接写 `-`。增时线只延长前一个内容，不会再次触发音符。

曲谱编辑器提供“延长 1 拍”按钮，会在光标处插入一个带安全空格的 `-`。

### 同音连线

传统乐谱中，两个音高完全相同的音符用延音线连接时，表示它们合并成一个长音，中间不能再次触发。KeyScore 将这种“同音连线”直接写成一个延长后的音符：

```text
1 -        一个持续两拍的 Do，只触发一次
1:2        与 1 - 完全等价
1 - | -    一个跨小节、持续三拍的 Do，只触发一次
```

下面这种写法不是同音连线：

```text
1 1        两个独立的 Do，需要触发两次
```

在短按触发模式下，同音连线只在开始时短按一次，延长部分用于等待；在持续按住模式下，按键从音符开始保持到合并后的总时值结束前。手工制作曲谱时不需要输入额外的弧线符号，使用 `-` 或 `:总时值` 即可。

### 不同音符连音

不同音高之间需要无缝衔接时，使用圆括号包围两个或更多音符：

```text
(1 2 3) 4
(H1:0.5 H2:0.5 H3)
(1 [2 4] | 3)
```

连音组具有以下规则：

- 圆括号只改变组内相邻音符的释放衔接，不改变音符时值、起点或曲谱总拍数。
- 连音组至少需要两个音符或和弦。
- 可以跨越普通换行和小节线 `|`。
- 不能嵌套，不能包含休止符 `0` 或段落停顿 `---`。
- 和弦可以作为组内成员，例如 `(1 [2 4] 3)`。
- 同音延音仍应写成 `1 -` 或 `1:2`，不能用圆括号代替。

在持续按住模式下，组内前一个音符会保持到后一个音符实际按下的同一时刻，实现 0ms 释放间隔。例如 BPM 120 时：

```text
(1 2)

0ms     按下 1
500ms   松开 1
500ms   按下 2
```

同一时刻始终先松开旧音，再按下新音，不产生按键重叠。短按触发模式仍使用固定按键时长，因此圆括号不会延长实际按键。若两个音符最终映射到同一个物理键，程序会保留配置中的“最小释放间隔”，防止第二次触发被游戏吞掉。跨音区或半音修饰键的连音，会让旧音保持到新音完成修饰键切换后的实际按下时刻。

相邻音符的起点间隔计算公式：

```text
起点间隔（毫秒） = 60000 ÷ BPM × 时值
```

例如 BPM 为 120：

| 写法 | 到下一个音符的间隔 |
|---|---:|
| `1` | 500 毫秒 |
| `1:0.5` | 250 毫秒 |
| `1:2` | 1000 毫秒 |

`:2` 和等价的增时线始终控制这个音符占用的时间以及下一个音符的开始时间。实际按键保持方式由当前配置方案的“音符输出方式”决定：

- **短按触发（推荐）**：按键只保持设置中的“按键时长”，默认 50ms；剩余时值用于等待。适合按一下就播放完整音效的游戏乐器。
- **持续按住**：按键保持到音符结束前，仅预留“最小释放间隔”。适合长按时间会改变发声长度的游戏。

例如 BPM 120 时，`1:2 2` 中的第二个音符在 1000ms 开始。短按触发模式下，第一个音符默认在 50ms 松开；持续按住且最小释放间隔为 10ms 时，第一个音符在 990ms 松开。

持续按住模式下，下面这段曲谱：

```text
1:2 0:2 2
```

在 BPM 120、最小释放间隔 10ms 时的时间轴为：

```text
0ms       按下 Do
990ms     松开 Do
1000ms    开始休止
2000ms    休止结束，按下 Re
```

其中 `1:2` 表示 Do 持续两拍，`0:2` 表示随后静音两拍。两者虽然都会推进两拍时间，但只有 `1:2` 会在持续按住模式下保持音符键。

输出方式保存在各自的配置方案中。切换方法为：主界面选择配置方案 → 点击“设置” → 在“播放参数”中选择“短按触发（推荐）”或“持续按住” → 保存。持续按住模式下，“按键时长”会自动禁用。旧配置方案默认使用短按触发。

### 段落停顿

普通换行和空行只用于整理曲谱排版，**不会产生任何停顿**。例如下面两种写法的播放效果完全相同：

```text
1 2 3 4
5 6 7 H1
```

```text
1 2 3 4 5 6 7 H1
```

需要精确停顿时使用休止符 `0`、`0:时值`；需要段落停顿时，必须使用独占一行的 `---`。

在文件头设置默认段落间隔，并在两个段落之间单独写一行 `---`：

```text
@section_gap 2

1 2 3 4 |
---
5 6 7 H1 |
```

上例会在第一段结束后停顿两拍。`---` 必须独占一行；未填写 `@section_gap` 时默认停顿一拍。普通换行、空行和 `|` 仍然只负责排版，不会自动产生停顿。

## 6. 和弦

方括号内的音符会同时演奏：

```text
[1 3 5]
[1 3 5]:2
[H1 H3 H5]:0.5
[#1 #2 #5]
```

和弦的时值必须写在右方括号后。不能给和弦内部的单个音符分别设置时值。

正确写法：

```text
[1 3 5]:2
```

错误写法：

```text
[1:2 3 5]
```

由于低音、高音和半音使用全局修饰键，同一个和弦必须使用相同的音区和半音状态：

```text
[1 3 5]       支持
[L1 L3 L5]    支持
[H1 H3 H5]    支持
[#1 #2 #5]    支持
[L1 H3]       不支持
[1 #3]        不支持
```

和弦中不能包含休止符 `0`。

## 7. 完整示例

```text
@title 示例曲谱
@bpm 100
@beat 4/4
@section_gap 2

1 2 3 4 | 5 6 7 H1 |
---
H1 H2 H3:2 | 0:2 |
L5 L6 L7 1 | #1 #2 3:2 |
[1 3 5]:2 | [H1 H3 H5]:2 |
```

## 8. 常见错误

### 音符没有用空格分开

```text
12345      错误
1 2 3 4 5  正确
```

### 使用了中文或小写音区字符

```text
高1  l1    错误
H1   L1    正确
```

### BPM 超出范围

```text
@bpm 10    错误
@bpm 100   正确
```

### 把换行当成停顿

下面两种写法的播放结果相同：

```text
1 2 3 4
5 6 7 H1
```

```text
1 2 3 4 5 6 7 H1
```

需要精确休止时使用 `0` 或带时值的休止符，例如 `0:2`；需要统一的段落停顿时使用独立一行的 `---`。

### 添加了说明文字或注释

当前版本没有注释语法。曲谱正文中不要填写歌词、括号说明或其他文字，否则语法检查会报错。

## 9. 制作建议

1. 先用纯数字录入主旋律，再补充高低音和半音。
2. 先用整数拍制作节奏骨架，再用 `:0.5`、`:0.25` 调整短音。
3. 每小节使用 `|` 分隔，每行保留 2～4 个小节，方便检查。
4. 先在较低 BPM 下试听，确认音符和修饰键正确后再恢复原速。
5. 连续同音漏奏时，适当减小“按键时长”或增大“最小释放间隔”。
6. 游戏响应较慢时，可以适当降低 BPM，并检查低音、高音、半音修饰键是否绑定正确。
