# KeyScore 方案设计

推荐软件名：**KeyScore（键谱）**

中文名可以直接叫：**键谱**
副标题：**游戏简谱自动演奏工具**

这个名字比较贴合你的功能：`Key` 代表键盘/鼠标输入，`Score` 代表曲谱、简谱。

整体方案建议这样设计。

> 实施状态（2026-09-22）：第一版已升级为多 Profile 架构。曲谱与游戏配置彼此独立；当前已实现“音级 + 修饰键”“三行音区直接映射”“五行音区直接映射”“每个音符直接映射”四种编译模式，支持圆括号不同音符连音及持续按住模式下的 0ms 衔接，并已加入游戏内键鼠演奏录制、量化和转谱闭环。独立曲谱编辑器同时提供文本与钢琴卷帘视图，卷帘已支持选择、框选、绘制、网格吸附、撤销和双向拖拽确定音符拍数。主导航现为“曲谱、按键映射、设置、关于”，设置页保留“外观、快捷键”二级分类，并已实现全局快捷键、主题和悬浮层选项持久化。下文较早的单一默认配置示例仅用于说明基础概念，应以“多游戏 Profile 架构”和“游戏演奏录制”章节为准。

### 一、软件定位

键谱是一款面向游戏乐器演奏场景的桌面工具。

用户可以自行配置：

* Do、Re、Mi、Fa、Sol、La、Si 对应的键盘按键
* 低音、中音、高音对应的键盘或鼠标操作
* 音区采用“切换模式”还是“组合模式”
* 演奏速度、按键时长、音符间隔

然后在软件中编辑或导入简谱，由软件自动转换成键盘和鼠标操作发送给游戏。

整体流程：

```text
简谱编辑 / 导入
        ↓
简谱解析
        ↓
音符 + 音区 + 时值
        ↓
读取键鼠映射
        ↓
生成操作序列
        ↓
按照节奏执行
        ↓
游戏内完成演奏
```

### 二、主界面设计

主窗口采用“固定顶部播放栏 + 左侧导航 + 分页工作区”。顶部保留上一首、播放/暂停、停止、下一首、录制、快捷键提示和当前 Profile 选择，不再重复提供设置入口。左侧导航固定为“曲谱、按键映射、设置、关于”四页：曲谱页合并曲谱管理、只读预览与播放状态；按键映射页集中管理 Profile、键位和演奏参数；设置页通过二级分类管理外观与全局快捷键；关于页集中展示版本、运行环境和本地数据目录。曲谱编辑继续使用独立窗口。

目标窗口句柄仅作为后台前台保护状态，不在正常播放界面常驻展示。主窗口不保留底部操作提示栏，避免持续占用垂直空间；等待用户切换到游戏、失去前台导致自动暂停或重新检测到游戏时，使用对话框、临时提示或悬浮层反馈。

#### 2.1 双主题视觉系统

界面提供两套可运行时切换的全局主题，默认使用 **Windows 11 Fluent**：浅色中性背景、白色内容面、蓝色强调色、圆角控件和清晰的层级关系。另一套为 **游戏电竞风**：深蓝黑背景、青色主强调和紫色选中态，保持高对比度，但不改变控件布局和操作路径。

主题入口位于“设置 → 外观 → 界面风格”。下拉框切换时即时预览并写入应用数据目录的 `app_settings.json`，下次启动自动恢复。主题属于应用级偏好，不写入按键 Profile，因此切换配置方案不会连带改变界面风格。

样式由全局 `QApplication` 统一管理，覆盖主窗口、曲谱编辑器、按键录入、标准对话框及播放悬浮层。业务控件只声明语义对象名或状态属性，不直接写死颜色；这样两套主题可共享布局和交互，同时避免新增窗口退回旧样式。

```text
┌──────────────────────────────────────────────────────────┐
│ KeyScore 键谱                           游戏配置：默认 ▼ │
├─────────────┬─────────────────────────────┬──────────────┤
│             │                             │ 输入映射      │
│   曲谱库     │         简谱编辑器           │              │
│             │                             │ Do    A      │
│ 小星星      │  @title 小星星               │ Re    S      │
│ 欢乐颂      │  @bpm 100                   │ Mi    D      │
│ 自定义曲谱   │                             │ Fa    F      │
│             │  1 1 5 5 | 6 6 5:2 |        │ Sol   G      │
│ [+ 新建]    │  4 4 3 3 | 2 2 1:2 |        │ La    H      │
│             │                             │ Si    J      │
│             │                             │              │
│             │                             │ 低音 左键     │
│             │                             │ 中音 中键     │
│             │                             │ 高音 右键     │
├─────────────┴─────────────────────────────┴──────────────┤
│ 当前：Sol   中音   → G                                   │
│                                                          │
│ 进度 ━━━━━━━━━━━━━━━●━━━━━━━━━━━━━━━━━━  35%             │
│                                                          │
│ BPM  [100]    按键时长 [50ms]                            │
│                                                          │
│        ▶ 开始      ⏸ 暂停      ■ 停止                    │
│                                                          │
│                   F10 紧急停止                            │
└──────────────────────────────────────────────────────────┘
```

#### 2.2 游戏演奏录制（已实现）

录制采用“物理输入事件 → 当前 Profile 反向解码 → 节拍量化 → 文本曲谱”的结构，不录屏、不读取游戏内存，也不通过麦克风识谱。Windows 全局键鼠监听运行在独立消息线程中，只在用户明确开始录制、且目标游戏位于前台时接收事件；监听回调只发送带高精度时间戳的扫描码或鼠标键，Qt 主线程负责状态管理和界面更新。

```text
F8 / 录制演奏
    ↓
录制设置（曲名、BPM、拍号、量化、和弦容差）
    ↓
等待游戏窗口 → 3 秒倒计时
    ↓
按当前 Profile 反向解码
    ↓
和弦聚合、时值量化、休止与连音识别
    ↓
录制整理 → 保存新曲谱 → 打开编辑器
```

短按触发模式无法从固定的短暂 KeyUp 判断发音是否持续，因此以相邻音符起点间隔作为曲谱时值；持续按住模式使用 KeyDown/KeyUp 计算发音时值，并把释放到下一次按下之间的空白生成休止。持续按住模式下，两个相邻单音的释放和下一次按下相差不超过连音容差时，生成圆括号语法并沿用现有 0ms 无缝播放规则。

人工和弦不要求完全同时，默认把 35ms 内的音符起点聚合为同一和弦。原始毫秒位置按照 BPM 转为拍数，默认量化到 0.25 拍，并按照 `@beat` 自动插入小节线。生成结果必须再次通过现有曲谱解析器校验，用户可以在“录制整理”窗口直接修改后再写入曲谱库。

为避免误录和歧义，播放与录制互斥，软件注入输入会被丢弃；只解释当前 Profile 中已经配置的物理键；重复绑定会阻止录制；当前配置的录制、播放和停止快捷键主键不能再作为该 Profile 的演奏键。目标游戏失焦时会关闭活动音符并暂停，切回原窗口后继续，暂停区间不进入曲谱时间轴。

#### 2.3 设置分类与关于页（已实现）

设置页采用左侧二级分类列表和右侧内容堆栈，当前分类为“外观、快捷键”。外观页管理 Fluent 与游戏电竞主题，并提供播放悬浮层开关、1～10 秒倒计时时长和倒计时悬浮提示开关；两个开关默认开启，倒计时默认 3 秒。关闭倒计时提示只隐藏视觉窗口，不跳过安全倒计时与前台窗口校验。

快捷键页允许修改录制、播放/暂停/继续、紧急停止三个全局快捷键，以及仅在卷帘页生效的新音符拍数正向、反向切换快捷键，后两者默认分别为 `D`、`A`。常用拍数列表、循环顺序和默认拍数均可配置，默认列表为 1/4、1/2、3/4、1、2、4 拍，新音符默认使用 1/4 拍。全局快捷键通过 Win32 `RegisterHotKey` 注册；编辑器快捷键使用窗口级 `QShortcut`。

### 三、按键映射系统

这是软件最核心的部分。

音符映射：

| 音符  | 默认按键 |
| --- | ---- |
| Do  | A    |
| Re  | S    |
| Mi  | D    |
| Fa  | F    |
| Sol | G    |
| La  | H    |
| Si  | J    |

音区映射：

| 修饰 | 默认操作 |
| -- | ---- |
| 低音 | 按住鼠标左键 |
| 中音 | 不按任何修饰键 |
| 高音 | 按住鼠标右键 |
| 半音 | 按住鼠标中键 |

这里不要把绑定类型写死。

每个绑定都应该支持：

```text
键盘任意键
鼠标左键
鼠标中键
鼠标右键
鼠标侧键1
鼠标侧键2
```

后期甚至可以扩展游戏手柄。

### 三点一、多游戏 Profile 架构（当前实施标准）

KeyScore 不将 `Z`、鼠标右键等物理输入写进曲谱。曲谱只描述“低音 Do”“高音 #Sol”等音乐语义；播放前由用户选择的 Profile 将音符编译为具体键盘、鼠标动作。因此同一份曲谱可以适配不同游戏和不同玩家的个人键位。

```text
文本简谱
    ↓
NoteEvent（音级 + 音区 + 半音 + 时值 + 是否连向下一音）
    ↓
GameProfile（映射规则和个人绑定）
    ↓
TimedInputEvent（绝对时间输入事件）
```

Profile 是独立文件，保存于应用数据目录的 `profiles/*.ksprofile.json`。旧版的单一 `profile.json` 会在首次启动时自动迁移为一个同名方案，避免用户原有设置丢失。

当前选中的方案记录在应用数据目录的 `profile_state.json`，应用重启时优先恢复该方案。保存 Profile 使用临时文件替换，避免写入中断留下半份 JSON。

当前支持的映射模式：

| `mapping_mode` | 适用布局 | 编译规则 |
| --- | --- | --- |
| `degree_modifier` | 三角洲等音级键布局 | 1～7 映射主按键；低/高音、半音用按住修饰键叠加 |
| `direct_note` | 每个音独立键位的游戏 | 五个音区各显示自然音和半音行，半音行仅提供 #1、#2、#4、#5、#6 |
| `row_octave` | 三行键盘音区布局 | 设置界面严格显示低、中、高三行，每行横向排列 1～7，不提供半音行或修饰键 |
| `five_row_octave` | 五行键盘音区布局 | 显示倍低、低、中、高、倍高五行，对应 `LL`、`L`、无前缀、`H`、`HH`，不提供半音行 |

`direct_note` 允许一个和弦混合五个音区和半音；`row_octave` 与 `five_row_octave` 允许跨音区自然音，但不支持半音；`degree_modifier` 的修饰键作用于整组输入，所以混合音区或自然音/半音和弦会在播放前报出明确错误。

Profile 核心字段：

```json
{
  "version": 4,
  "name": "三角洲口琴 - 我的键位",
  "mapping_mode": "degree_modifier",
  "note_output_mode": "tap",
  "notes": { "1": { "kind": "keyboard", "label": "Z" } },
  "zones": { "low": { "kind": "mouse", "label": "鼠标左键" } },
  "semitone": { "kind": "mouse", "label": "鼠标中键" },
  "direct_notes": {
    "middle:0:1": { "kind": "keyboard", "label": "A" }
  }
}
```

`direct_notes` 的键名是 `音区:是否半音:音级`。例如 `high:1:5` 表示高音 #Sol。独立半音只有 #1、#2、#4、#5、#6；#3 自动规范为 4，#7 自动规范为高一音区 1。所有映射模式均固定使用 `1～7`；高音 Do 始终写作 `H1`，不设置独立的第八音级。Profile 绑定支持单键、单独修饰键和标准修饰键组合，旧版单键配置读取后自动兼容。

`note_output_mode` 是配置方案级别的音符输出方式：`tap` 表示固定时长短按，`hold` 表示根据曲谱时值持续按住。每个游戏配置可以独立选择。

配置方案管理界面应提供：

```text
当前方案下拉选择
├── 默认配置
├── 三角洲口琴 - 我的键位
├── 原神诗琴
├── 新建配置方案…（从当前方案复制）
└── 导入配置方案…（校验并复制 `.ksprofile.json`）
```

导入方案必须严格校验 JSON 结构、映射模式和绑定字段；无效文件不能回退成默认配置后混入列表。有效方案复制进本地目录，重名时自动增加数字后缀，导入后立即显示在配置下拉栏并切换为当前方案。

按键映射页面顶部提供“方案名称”字段。名称不变时更新当前方案；使用新名称保存时保留原方案并另存为新的 Profile，然后同步加入顶部下拉栏并切换为当前方案。如果规范化后的文件名已经存在，则拒绝覆盖并提示用户。用户可在页面内切换映射模式，并立即看到相应的键位录入区域。直接映射模式未配置的音符不会猜测或静默跳过，而是在编译时报告“某音符没有配置按键”。

### 四、音区与半音修饰键

当前实施统一使用按住修饰键的方式，不再对中音发送鼠标中键。中音是什么都不按的默认状态，鼠标中键改为半音修饰键。下方早期的切换模式设计仅保留为历史记录，不再用于当前播放引擎。

“切换模式”适合这种游戏：

```text
右键
↓
进入高音区
↓
A
S
D
↓
继续都是高音
```

只有切换音区时才再次点击鼠标。

“组合模式”则是：

```text
高音 Do：

按住右键
↓
按 A
↓
松开 A
↓
松开右键
```

配置界面：

```text
音区模式

● 切换模式
○ 组合模式
```

### 五、简谱编辑器

不建议第一版追求完全标准的音乐排版。

内部采用容易输入、容易解析的文本简谱。

例如：

```text
@title 小星星
@bpm 100
@beat 4/4
@section_gap 2

1 1 5 5 | 6 6 5:2 |
---
4 4 3 3 | 2 2 1:2 |
```

音区建议：

```text
L1    低音 Do
L5    低音 Sol

1     中音 Do
5     中音 Sol

H1    高音 Do
H5    高音 Sol
```

例如：

```text
L5 L6 L7 | 1 2 3 | H1 H2 H3
```

半音使用 `#` 前缀，播放时按住半音修饰键：

```text
#1 #2 #5
#H1
```

休止符：

```text
0
0:0.5
0:2
```

普通换行、空行和小节线只用于排版，不改变播放时间。独立一行的 `---` 表示段落停顿，停顿拍数由 `@section_gap` 指定，未设置时默认一拍。编辑器提供半拍、一拍、两拍休止和段落停顿的快捷插入按钮。

时值：

```text
1       1拍
1:2     2拍
1:0.5   半拍
1:0.25  四分之一拍
```

同时支持传统简谱增时线，每个独立的 `-` 将前一个音符、和弦或休止符延长一拍。`1 - - -` 在解析阶段归一化为一个时值为四拍的音符，与 `1:4` 完全等价，不生成额外按键事件。增时线可以跨普通换行和小节线，但不能出现在曲谱开头或段落停顿 `---` 之后。

不同音高之间的连音使用圆括号表示：

```text
(1 2 3) 4
(H1:0.5 H2:0.5 H3)
(1 [2 4] | 3)
```

圆括号只标记组内相邻音符或和弦的衔接关系，不改变任何音符的开始拍位、时值或曲谱总拍数。连音组至少包含两个音符或和弦，可以跨普通换行和小节线，但不能嵌套，也不能包含休止符或段落停顿。同音延音仍使用 `-` 或 `:总时值`；圆括号内出现连续同音时仍需要重新触发。

这样比用各种点和横线更适合程序处理。

UI 展示时，再把它渲染成比较接近传统简谱的样子。

### 六、简谱编辑辅助

独立编辑器采用“钢琴卷帘 + 曲谱文本”双视图。文本页负责完整语法编辑、总时值快捷修改和结构符号插入；卷帘页负责可视化定位、选择、删除和新音符绘制。两个视图共享同一份 `ScoreDocument`，仅在切换页面或保存时执行必要的序列化，避免每次鼠标移动都重写文本。

卷帘包含两种互斥操作模式：

| 模式 | 默认操作 | 临时反转 |
|---|---|---|
| 选择（`V`） | 空白处拖动框选；点击音符选择 | 按住 `Alt` 拖动绘制 |
| 绘制（`B`） | 空白处按住并横向拖动绘制音符 | 按住 `Alt` 拖动框选 |

绘制起点和终点按照当前网格吸附。按下时锁定音高，横向拖动可以向左或向右确定总拍数；半透明预览块实时显示拍数，合法时使用主题强调色，发生时间重叠或和弦时值冲突时显示红色，松开后不提交无效结果。拖到视口边缘时自动横向滚动，单次绘制作为一个撤销步骤。点击已有音符始终优先选择，避免在绘制模式下误覆盖音符。

选择模式继续保留双击空白网格按当前默认拍数添加音符。工具栏提供量化网格、新音符默认拍数、撤销、重做、删除和横向缩放；新音符常用拍数及正反向切换快捷键由应用设置提供。

文本页提供总时值快捷按钮，以及小节线、增时线、休止符和段落停顿的插入入口。所有修改最终必须经过统一解析与序列化校验，保证卷帘和文本能够无损往返。

### 七、文件格式

当前曲谱的唯一持久化与交换格式为 UTF-8 `.txt`。文件同时保存元数据和简谱正文，便于直接检查、版本管理、复制与分享：

```text
@title 小星星
@bpm 100
@beat 4/4
@section_gap 1

1 1 5 5 | 6 6 5:2 |
4 4 3 3 | 2 2 1:2 |
```

曲谱不保存物理键位或 Profile 名称。同一份 `.txt` 曲谱可以在播放时搭配任意兼容的配置方案。文件名根据 `@title` 生成；发生重名时追加数字后缀，不覆盖已有曲谱。

应用持久化文件分工：

```text
scores/*.txt                 曲谱语义
profiles/*.ksprofile.json    游戏键位与演奏参数
profile_state.json           当前 Profile 选择
app_settings.json            主题、快捷键与悬浮层选项
```

当前版本不使用 `.kscore` 工程文件，避免曲谱和个人物理键位重新耦合。

### 八、配置文件

游戏按键映射不要和曲谱完全绑定。

单独建立“游戏配置”。

例如：

```text
配置方案
├── 默认
├── 游戏A
├── 游戏B
└── 我的自定义配置
```

游戏 A：

```text
Do   → A
Re   → S
Mi   → D
Fa   → F
Sol  → G
La   → H
Si   → J

低音 → 鼠标左键
中音 → 鼠标中键
高音 → 鼠标右键

模式 → 切换模式
```

这样一份《小星星》可以直接用于不同游戏，只需要换配置。

### 九、播放引擎

播放时不要直接一边解析一边按键。

建议先转换成统一动作序列：

```text
H5
```

转换成：

```text
切换高音
按 G
等待 1 拍
```

进一步形成：

```text
[
    MouseRight,
    KeyDown(G),
    Wait(50ms),
    KeyUp(G),
    Wait(550ms)
]
```

这样以后做暂停、继续、速度调整都会容易很多。

### 十、技术方案

Windows 桌面应用第一版我推荐：

```text
Python
+
PySide6
+
Win32 RegisterHotKey 与低级键鼠 Hook
```

结构：

```text
KeyScore
│
├─ UI
│  └─ PySide6
│
├─ Score
│  ├─ 简谱解析
│  ├─ 曲谱编辑
│  └─ 文件管理
│
├─ Input
│  ├─ 键盘模拟
│  ├─ 鼠标模拟
│  └─ 全局快捷键
│
├─ Player
│  ├─ BPM
│  ├─ 播放队列
│  ├─ 暂停
│  └─ 停止
│
└─ Config
   ├─ 游戏配置
   └─ 软件设置
```

第一版建议只做这 8 个功能：

1. Do～Si 按键自定义。
2. 低中高音键盘/鼠标自定义。
3. 低音、高音和半音按住修饰，中音无修饰。
4. 简谱编辑器。
5. `.txt` 曲谱导入、编辑与安全重命名。
6. BPM 和音符时值。
7. 开始、暂停、继续、停止。
8. F10 全局紧急停止。

软件名称最终我比较推荐：

**KeyScore**
**中文名：键谱**

如果希望更偏游戏工具风格，也可以考虑：

* GameScore
* KeyMuse
* PlayKey
* NoteKey
* 键音谱
* 游谱

其中 **KeyScore / 键谱** 最简洁，也比较方便后续做软件 Logo 和 UI 品牌。

### 十一、第一版实施方案（已确定）

第一版只实现前台游戏播放，暂不实现后台注入、DLL 注入或游戏进程读写。

输入输出方式：

```text
全局快捷键监听：Win32 `RegisterHotKey`
键盘输出：Windows SendInput + 扫描码
鼠标输出：Windows SendInput + MOUSEINPUT
前台检测：GetForegroundWindow
```

播放流程：

```text
在游戏内按 F9（无需选择游戏）
        ↓
内部自动记录当时的前台窗口，仅用于防止误输入
        ↓
显示 3、2、1 屏幕倒计时
        ↓
按绝对时间轴发送键鼠事件
        ↓
显示不抢占焦点的播放信息悬浮条
```

快捷键默认规则：

| 快捷键 | 功能 |
| --- | --- |
| F9 | 开始、暂停或继续 |
| F10 | 紧急停止并释放所有按键 |

用户不需要手动选择游戏或进程。开始播放后，软件只在内部校验前台窗口是否仍与按下 F9 时相同。如果前台窗口发生变化，必须立即暂停、释放所有已按下的键鼠按键，不得将曲谱内容输入到其他应用。

### 十二、悬浮层与极简主义界面

倒计时和播放信息使用独立悬浮窗口，窗口应具备：

```text
无边框
始终置顶
半透明背景
不进入任务栏
不抢占游戏焦点
鼠标点击穿透
```

推荐用户在无边框全屏或窗口化游戏中使用。独占全屏模式下不保证悬浮层能够显示。

倒计时屏幕中央显示：

```text
              3

       即将开始演奏
          《小星星》

        F10 随时停止
```

播放后收缩为小型悬浮条，采用“曲名 + BPM”“进度条”“状态 + 当前音符 + F9/F10 提示”三层布局。外层不保留默认边距，窗口高度必须覆盖卡片内边距和三层内容，避免底部状态文字被裁切。

视觉方向：

```text
默认近黑色背景
主要使用黑、白、灰，不使用大面积品牌强调色
主视图只保留窄歌单和曲谱预览，不使用三栏卡片
低频的应用级配置收进设置页面，Profile 相关配置集中到按键映射页面
0～5px 小圆角
无阴影、无渐变、尽量无边框
仅保留必要的状态反馈
悬浮层尺寸尽量小，避免遮挡游戏画面
```

第一版默认色板：

| 用途 | 颜色 |
| --- | --- |
| 主背景 | `#101112` |
| 次级背景 | `#171819` |
| 分隔与轨道 | `#242628` |
| 主要文字 | `#ECEDEE` |
| 次要文字 | `#777B81` |
| 主操作 | `#ECEDEE` |
| 错误状态 | `#D86A6A` |

### 十三、播放引擎约束

播放引擎使用单调时钟和绝对时间戳，不使用连续相对 `sleep` 作为乐曲时间基准，以避免长时间播放后误差累积。

内部处理链路：

```text
简谱文本
    ↓
Score / NoteEvent
    ↓
TimedInputEvent
    ↓
SendInputBackend
```

无论用户停止、切换窗口、发生异常还是按下 F10，最终都必须执行 `release_all`，确保不留下卡键或鼠标按键未释放的状态。

### 十四、歌单、独立编辑器与按键录入

主窗口左侧设置窄歌单栏，用于显示应用数据目录中已有或已导入的所有 `.txt` 曲谱。导入操作将源文件复制到本地曲谱库，避免原文件移动后歌单失效。选中曲谱后可通过歌单底部“删除”按钮或“文件 → 删除当前曲谱”删除；删除前必须二次确认，且只允许删除曲谱库目录内的 `.txt` 文件。

```text
歌单
├── 小星星
├── 欢乐颂
└── 自定义曲谱

[新建] [导入]
```

单击曲谱后，右侧只读预览显示曲名、BPM、拍数、音符数与原始简谱，列表可按最近修改时间或名称排序并记住选择；名称排序使用中文区域排序器，兼容英文 A–Z 和中文拼音首字母。新建曲谱以内存草稿打开，默认只有一个中音 `1`，只有点击保存后才写入曲谱库。双击曲谱或点击“编辑”时，打开独立曲谱编辑窗口。编辑器保留实时语法检查；点击保存先弹出确认，确认后根据 `@title` 安全重命名本地 `.txt` 文件，重名时追加数字而不覆盖，然后刷新主窗口并关闭编辑窗口。

按键映射页面中，以按钮形式显示所有映射：

```text
1 Do    [A]
2 Re    [S]
3 Mi    [D]
4 Fa    [F]
5 Sol   [G]
6 La    [H]
7 Si    [J]
低音    [鼠标左键]
中音    [默认，无按键]
高音    [鼠标右键]
半音    [鼠标中键]
```

点击任意映射按钮后，弹出按键录入窗口，接收下一个键盘键、鼠标左/中/右键或侧键。键盘绑定保存物理扫描码和扩展键标志，保存后写入当前 `profiles/*.ksprofile.json`，下次启动自动恢复。方案保存成功时，主内容区顶部显示一秒钟且不抢焦点的成功轻提示；连续保存会重新计时，Fluent 与电竞主题分别使用匹配的成功状态配色。

### 十五、按键间隔与时长控制

播放时间拆分为六个独立概念：

1. **音符起点间隔**：由 BPM 和简谱时值决定，计算公式为 `60000 / BPM × duration_beats`。例如 BPM 120 时，`1` 的起点间隔是 500ms，`1:0.5` 是 250ms；`1 - - -` 与 `1:4` 一样占用四拍。
2. **音符输出方式**：每个 Profile 独立保存。`tap` 为短按触发，使用固定按键时长；`hold` 为持续按住，按键保持到音符结束前。
3. **按键时长**：从 KeyDown 到 KeyUp 的固定时间，默认 50ms，仅在短按触发模式生效。游戏只要识别触发而不表现延音时，宜使用 30～70ms。
4. **最小释放间隔**：从 KeyUp 到下一个拍点的最小时间，默认 10ms。这一参数尤其影响连续相同音符，用于防止两次按键被游戏合并成一次。
5. **休止与段落间隔**：`0:时值` 表示精确休止；独立一行的 `---` 按 `@section_gap` 推进时间，但不生成任何按键事件。
6. **不同音符连音**：圆括号组内的相邻异键音符在持续按住模式下使用 0ms 释放间隔，旧音在下一音实际按下的同一时间释放；短按触发模式不改变固定短按行为。

短按触发模式的实际按键时长按以下规则截断：

```text
actual_hold_ms = min(
    key_hold_ms,
    当前音符剩余时间 - key_gap_ms
)
```

持续按住模式不使用固定按键时长：

```text
actual_hold_ms = 当前音符剩余时间 - key_gap_ms
```

持续按住模式下，圆括号连音组内的异键连接改用：

```text
current_key_up_time = next_key_down_time
```

同一时间点仍按“先释放、后按下”排序，不引入按键重叠。若相邻音符最终映射到同一物理键，则自动恢复普通 `key_gap_ms`，确保游戏能够识别第二次按键。使用全局音区或半音修饰键时，旧音会保持到下一音完成修饰键切换后的实际按下时刻；修饰键本身仍按安全顺序切换。

按键映射页面的“演奏参数”提供“音符输出方式”下拉框。选择“短按触发（推荐）”时启用“按键时长”；选择“持续按住”时禁用“按键时长”，由曲谱时值决定释放时间。保存方案前先停止当前播放并清除旧时间轴，保存后下一次播放立即生效。该字段以 `note_output_mode` 写入 Profile；旧 Profile 缺少该字段时默认迁移为 `tap`，确保行为不变。

因此即使用户将短按时长设得过大，播放器也会在下一个音符前自动松键。如果游戏出现漏键，优先将最小释放间隔从 10ms 逐步提高到 20～40ms；如果游戏没有识别短按，则逐步增加按键时长。只有游戏确实根据长按时间持续发声时，才选择“持续按住”。

### 十六、测试版打包与发布

Windows 测试版统一使用项目根目录的 `build_test.ps1` 构建，目标平台为 Windows x64。构建工具使用 PyInstaller，但 PyInstaller 仅作为开发期构建工具，不属于应用运行依赖。

打包采用 `onedir` 目录模式，不采用单文件模式。原因是配置方案、曲谱和当前方案状态都需要在运行期间持久化；目录模式可以让 EXE 同级的 `data` 保持为普通可写目录，也便于测试人员直接检查、导入和备份数据。

```text
dist/KeyScore-Test/
├── KeyScore-Test.exe
├── _internal/                 Python 与必要运行依赖
└── data/
    ├── profiles/              随测试版发布的配置方案
    └── scores/                随测试版发布的示例曲谱
```

冻结运行时的数据目录固定为 `KeyScore-Test.exe` 同级的 `data`。源码方式启动时仍使用项目根目录的 `data`；显式设置 `KEYSCORE_DATA_DIR` 时优先使用该路径。

依赖收集遵循最小化原则：

1. 只保留应用实际使用的 PySide6 `QtCore`、`QtGui`、`QtWidgets`、Windows 平台插件和 Python 运行时。
2. 排除 Qt Multimedia、Network、OpenGL、PDF、QML、Quick、WebEngine 和 tkinter 等当前功能未使用的模块。
3. 删除虚拟键盘、图片格式、通用触摸等未使用插件，只保留 `qwindows.dll` 和 Windows 样式插件。
4. Qt 翻译只保留简体中文文件。
5. 构建期间隔离系统 `PATH`，防止 Anaconda、PDF 工具或其他软件的同名 DLL 被错误收集；构建后额外移除误收集的 ICU DLL。
6. 发布包包含 `data/profiles/*.ksprofile.json` 和 `data/scores/*.txt`，不包含设备相关的 `profile_state.json` 或临时文件。

标准构建命令：

```powershell
.\build_test.ps1
```

交付前必须完成以下验证：

```text
运行全部单元测试
检查配置方案和曲谱已复制到 data
检查未混入非项目 ICU DLL
启动 EXE 并保持运行至少 5 秒
关闭测试进程后生成 Win64 ZIP
```

测试版发布到 GitHub Releases，并标记为预发布。标签使用 `v主版本.次版本.修订版本-test.序号`，例如 `v0.1.0-test.1`。发布附件使用 `KeyScore-Test-win64.zip`；使用者必须完整解压后运行，不能只复制 EXE。
