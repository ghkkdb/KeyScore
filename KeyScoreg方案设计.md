推荐软件名：**KeyScore（键谱）**

中文名可以直接叫：**键谱**
副标题：**游戏简谱自动演奏工具**

这个名字比较贴合你的功能：`Key` 代表键盘/鼠标输入，`Score` 代表曲谱、简谱。

整体方案建议这样设计。

### 一、软件定位

键谱是一款面向游戏乐器演奏场景的桌面工具。

用户可以自行配置：

* Do、Re、Mi、Fa、Sol、La、Si 对应的键盘按键
* 低音、中音、高音对应的键盘或鼠标操作
* 音区采用“切换模式”还是“组合模式”
* 演奏速度、按键时长、音符间隔

然后在软件中编辑或导入简谱，由软件自动转换成键盘和鼠标操作发送给游戏。

整体流程：

```text
简谱编辑 / 导入
        ↓
简谱解析
        ↓
音符 + 音区 + 时值
        ↓
读取键鼠映射
        ↓
生成操作序列
        ↓
按照节奏执行
        ↓
游戏内完成演奏
```

### 二、主界面设计

主界面采用极简双区布局：左侧是窄歌单栏，显示已有和导入的曲谱；右侧是当前曲谱的只读预览和播放信息。曲谱编辑放在独立窗口，键位和音区配置放入设置对话框。下方早期三栏草图仅保留为过程记录，不再作为实施标准。

```text
┌──────────────────────────────────────────────────────────┐
│ KeyScore 键谱                           游戏配置：默认 ▼ │
├─────────────┬─────────────────────────────┬──────────────┤
│             │                             │ 输入映射      │
│   曲谱库     │         简谱编辑器           │              │
│             │                             │ Do    A      │
│ 小星星      │  @title 小星星               │ Re    S      │
│ 欢乐颂      │  @bpm 100                   │ Mi    D      │
│ 自定义曲谱   │                             │ Fa    F      │
│             │  1 1 5 5 | 6 6 5:2 |        │ Sol   G      │
│ [+ 新建]    │  4 4 3 3 | 2 2 1:2 |        │ La    H      │
│             │                             │ Si    J      │
│             │                             │              │
│             │                             │ 低音 左键     │
│             │                             │ 中音 中键     │
│             │                             │ 高音 右键     │
├─────────────┴─────────────────────────────┴──────────────┤
│ 当前：Sol   中音   → G                                   │
│                                                          │
│ 进度 ━━━━━━━━━━━━━━━●━━━━━━━━━━━━━━━━━━  35%             │
│                                                          │
│ BPM  [100]    按键时长 [50ms]                            │
│                                                          │
│        ▶ 开始      ⏸ 暂停      ■ 停止                    │
│                                                          │
│                   F10 紧急停止                            │
└──────────────────────────────────────────────────────────┘
```

### 三、按键映射系统

这是软件最核心的部分。

音符映射：

| 音符  | 默认按键 |
| --- | ---- |
| Do  | A    |
| Re  | S    |
| Mi  | D    |
| Fa  | F    |
| Sol | G    |
| La  | H    |
| Si  | J    |
| 高音 Do（8） | K |

音区映射：

| 修饰 | 默认操作 |
| -- | ---- |
| 低音 | 按住鼠标左键 |
| 中音 | 不按任何修饰键 |
| 高音 | 按住鼠标右键 |
| 半音 | 按住鼠标中键 |

这里不要把绑定类型写死。

每个绑定都应该支持：

```text
键盘任意键
鼠标左键
鼠标中键
鼠标右键
鼠标侧键1
鼠标侧键2
```

后期甚至可以扩展游戏手柄。

### 四、音区与半音修饰键

当前实施统一使用按住修饰键的方式，不再对中音发送鼠标中键。中音是什么都不按的默认状态，鼠标中键改为半音修饰键。下方早期的切换模式设计仅保留为历史记录，不再用于当前播放引擎。

“切换模式”适合这种游戏：

```text
右键
↓
进入高音区
↓
A
S
D
↓
继续都是高音
```

只有切换音区时才再次点击鼠标。

“组合模式”则是：

```text
高音 Do：

按住右键
↓
按 A
↓
松开 A
↓
松开右键
```

配置界面：

```text
音区模式

● 切换模式
○ 组合模式
```

### 五、简谱编辑器

不建议第一版追求完全标准的音乐排版。

内部采用容易输入、容易解析的文本简谱。

例如：

```text
@title 小星星
@bpm 100
@beat 4/4
@section_gap 2

1 1 5 5 | 6 6 5:2 |
---
4 4 3 3 | 2 2 1:2 |
```

音区建议：

```text
L1    低音 Do
L5    低音 Sol

1     中音 Do
5     中音 Sol

H1    高音 Do
H5    高音 Sol
```

例如：

```text
L5 L6 L7 | 1 2 3 | H1 H2 H3
```

半音使用 `#` 前缀，播放时按住半音修饰键：

```text
#1 #2 #5
#H1
```

休止符：

```text
0
0:0.5
0:2
```

普通换行、空行和小节线只用于排版，不改变播放时间。独立一行的 `---` 表示段落停顿，停顿拍数由 `@section_gap` 指定，未设置时默认一拍。编辑器提供半拍、一拍、两拍休止和段落停顿的快捷插入按钮。

时值：

```text
1       1拍
1:2     2拍
1:0.5   半拍
1:0.25  四分之一拍
```

这样比用各种点和横线更适合程序处理。

UI 展示时，再把它渲染成比较接近传统简谱的样子。

### 六、简谱编辑辅助

编辑器最好带几个辅助功能：

```text
[低音] [中音] [高音]

[1 Do] [2 Re] [3 Mi] [4 Fa]
[5 Sol] [6 La] [7 Si] [8 高音 Do]

[休止符]

[1/4拍] [1/2拍] [1拍] [2拍]
```

所以不会写格式的人也可以直接点按钮插入简谱。

例如点击：

```text
高音
→
5
→
2拍
```

自动生成：

```text
H5:2
```

### 七、文件格式

建议使用两个核心格式。

软件工程格式：

```text
.kscore
```

例如：

```text
小星星.kscore
```

内部采用 JSON：

```json
{
  "version": 1,
  "title": "小星星",
  "bpm": 100,
  "beat": "4/4",
  "profile": "默认配置",
  "score": "1 1 5 5 | 6 6 5:2"
}
```

这个文件负责完整保存项目。

另外支持：

```text
.txt
```

方便复制和分享：

```text
@title 小星星
@bpm 100

1 1 5 5 | 6 6 5:2 |
4 4 3 3 | 2 2 1:2 |
```

所以建议最终：

```text
.kscore   默认工程文件
.txt      简谱交换文件
.json     高级导入导出
```

### 八、配置文件

游戏按键映射不要和曲谱完全绑定。

单独建立“游戏配置”。

例如：

```text
配置方案
├── 默认
├── 游戏A
├── 游戏B
└── 我的自定义配置
```

游戏 A：

```text
Do   → A
Re   → S
Mi   → D
Fa   → F
Sol  → G
La   → H
Si   → J

低音 → 鼠标左键
中音 → 鼠标中键
高音 → 鼠标右键

模式 → 切换模式
```

这样一份《小星星》可以直接用于不同游戏，只需要换配置。

### 九、播放引擎

播放时不要直接一边解析一边按键。

建议先转换成统一动作序列：

```text
H5
```

转换成：

```text
切换高音
按 G
等待 1 拍
```

进一步形成：

```text
[
    MouseRight,
    KeyDown(G),
    Wait(50ms),
    KeyUp(G),
    Wait(550ms)
]
```

这样以后做暂停、继续、速度调整都会容易很多。

### 十、技术方案

Windows 桌面应用第一版我推荐：

```text
Python
+
PySide6
+
pynput
```

结构：

```text
KeyScore
│
├─ UI
│  └─ PySide6
│
├─ Score
│  ├─ 简谱解析
│  ├─ 曲谱编辑
│  └─ 文件管理
│
├─ Input
│  ├─ 键盘模拟
│  ├─ 鼠标模拟
│  └─ 全局快捷键
│
├─ Player
│  ├─ BPM
│  ├─ 播放队列
│  ├─ 暂停
│  └─ 停止
│
└─ Config
   ├─ 游戏配置
   └─ 软件设置
```

第一版建议只做这 8 个功能：

1. Do～Si 以及第八个高音 Do 按键自定义。
2. 低中高音键盘/鼠标自定义。
3. 低音、高音和半音按住修饰，中音无修饰。
4. 简谱编辑器。
5. `.kscore` 和 `.txt` 导入导出。
6. BPM 和音符时值。
7. 开始、暂停、继续、停止。
8. F10 全局紧急停止。

软件名称最终我比较推荐：

**KeyScore**
**中文名：键谱**

如果希望更偏游戏工具风格，也可以考虑：

* GameScore
* KeyMuse
* PlayKey
* NoteKey
* 键音谱
* 游谱

其中 **KeyScore / 键谱** 最简洁，也比较方便后续做软件 Logo 和 UI 品牌。

### 十一、第一版实施方案（已确定）

第一版只实现前台游戏播放，暂不实现后台注入、DLL 注入或游戏进程读写。

输入输出方式：

```text
全局快捷键监听：pynput
键盘输出：Windows SendInput + 扫描码
鼠标输出：Windows SendInput + MOUSEINPUT
前台检测：GetForegroundWindow
```

播放流程：

```text
在游戏内按 F9（无需选择游戏）
        ↓
内部自动记录当时的前台窗口，仅用于防止误输入
        ↓
显示 3、2、1 屏幕倒计时
        ↓
按绝对时间轴发送键鼠事件
        ↓
显示不抢占焦点的播放信息悬浮条
```

快捷键默认规则：

| 快捷键 | 功能 |
| --- | --- |
| F9 | 开始、暂停或继续 |
| F10 | 紧急停止并释放所有按键 |

用户不需要手动选择游戏或进程。开始播放后，软件只在内部校验前台窗口是否仍与按下 F9 时相同。如果前台窗口发生变化，必须立即暂停、释放所有已按下的键鼠按键，不得将曲谱内容输入到其他应用。

### 十二、悬浮层与极简主义界面

倒计时和播放信息使用独立悬浮窗口，窗口应具备：

```text
无边框
始终置顶
半透明背景
不进入任务栏
不抢占游戏焦点
鼠标点击穿透
```

推荐用户在无边框全屏或窗口化游戏中使用。独占全屏模式下不保证悬浮层能够显示。

倒计时屏幕中央显示：

```text
              3

       即将开始演奏
          《小星星》

        F10 随时停止
```

播放后收缩为小型悬浮条，显示曲名、进度、当前音符、当前音区、BPM 以及 F9/F10 提示。

视觉方向：

```text
默认近黑色背景
主要使用黑、白、灰，不使用大面积品牌强调色
主视图只保留窄歌单和曲谱预览，不使用三栏卡片
低频配置收进设置对话框
0～5px 小圆角
无阴影、无渐变、尽量无边框
仅保留必要的状态反馈
悬浮层尺寸尽量小，避免遮挡游戏画面
```

第一版默认色板：

| 用途 | 颜色 |
| --- | --- |
| 主背景 | `#101112` |
| 次级背景 | `#171819` |
| 分隔与轨道 | `#242628` |
| 主要文字 | `#ECEDEE` |
| 次要文字 | `#777B81` |
| 主操作 | `#ECEDEE` |
| 错误状态 | `#D86A6A` |

### 十三、播放引擎约束

播放引擎使用单调时钟和绝对时间戳，不使用连续相对 `sleep` 作为乐曲时间基准，以避免长时间播放后误差累积。

内部处理链路：

```text
简谱文本
    ↓
Score / NoteEvent
    ↓
TimedInputEvent
    ↓
SendInputBackend
```

无论用户停止、切换窗口、发生异常还是按下 F10，最终都必须执行 `release_all`，确保不留下卡键或鼠标按键未释放的状态。

### 十四、歌单、独立编辑器与按键录入

主窗口左侧设置窄歌单栏，用于显示应用数据目录中已有或已导入的所有 `.txt` 曲谱。导入操作将源文件复制到本地曲谱库，避免原文件移动后歌单失效。

```text
歌单
├── 小星星
├── 欢乐颂
└── 自定义曲谱

[新建] [导入]
```

单击曲谱后，右侧只读预览显示曲名、BPM、拍数、音符数与原始简谱。双击曲谱或点击“编辑”时，打开独立曲谱编辑窗口。编辑器保留实时语法检查，保存后主窗口自动刷新歌单和预览。

设置对话框中，以按钮形式显示所有映射：

```text
1 Do    [A]
2 Re    [S]
3 Mi    [D]
4 Fa    [F]
5 Sol   [G]
6 La    [H]
7 Si    [J]
8 高音 Do [K]

低音    [鼠标左键]
中音    [默认，无按键]
高音    [鼠标右键]
半音    [鼠标中键]
```

点击任意映射按钮后，弹出按键录入窗口，接收下一个键盘键、鼠标左/中/右键或侧键。键盘绑定保存物理扫描码和扩展键标志，保存设置后写入 `profile.json`，下次启动自动恢复。

### 十五、按键间隔与时长控制

播放时间拆分为四个独立概念：

1. **音符起点间隔**：由 BPM 和简谱时值决定，计算公式为 `60000 / BPM × duration_beats`。例如 BPM 120 时，`1` 的起点间隔是 500ms，`1:0.5` 是 250ms。
2. **按键时长**：从 KeyDown 到 KeyUp 的时间，默认 50ms。游戏只要识别触发而不表现延音时，宜使用 30～70ms。
3. **最小释放间隔**：从 KeyUp 到下一个拍点的最小时间，默认 10ms。这一参数尤其影响连续相同音符，用于防止两次按键被游戏合并成一次。
4. **休止与段落间隔**：`0:时值` 表示精确休止；独立一行的 `---` 按 `@section_gap` 推进时间，但不生成任何按键事件。

实际按键时长按以下规则截断：

```text
actual_hold_ms = min(
    key_hold_ms,
    当前音符剩余时间 - key_gap_ms
)
```

因此即使用户将按键时长设得过大，播放器也会在下一个音符前自动松键。如果游戏出现漏键，优先将最小释放间隔从 10ms 逐步提高到 20～40ms；如果游戏没有识别短按，则逐步增加按键时长。
